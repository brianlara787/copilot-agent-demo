# Copilot Agent Demo

A small Python calculator demonstrating addition, subtraction, multiplication, and division with a zero check.

## Run the calculator

```bash
python main.py
```

The program prompts for two numbers and an operation (`+`, `-`, `*`, or `/`).
